using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;

public class Targets : MonoBehaviour
{

//	private List <Transform> allTargets;
//	private int touchingAgents = 100; 

	void Start ()
	{


//		allTargets = gameObject.GetComponentsInChildren<Transform> ().Skip (1).ToList ();
		//Debug.Log (allTargets.Count);

//		for (int i = 0; i < allTargets.Count; i++) {
//			Light newLight = allTargets[i].gameObject.AddComponent<Light> () as Light;
//			newLight.color = Color.blue;
//			newLight.intensity = 10; 
//			newLight.range = touchingAgents;
//
//		}
			
	}

}